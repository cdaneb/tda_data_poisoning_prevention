% =====================================================================
%  MathFest 2026 Poster -- 36 in x 36 in
%  "What Can a Persistence Diagram See?"
%  Christian Dane Beels, advised by Dr. Joseph Dorta
%  Template base: Sofia Jijon (https://sjijon.github.io)
%
%  LAYOUT (revised): outer columns 0.66 / 0.34.
%    - 0.66 column = subcolumns (Motivation etc. | Methods I+II), with the
%      References block BELOW them so it spans two subcolumns only.
%    - 0.34 column runs to the bottom and ends with Contact, so Contact and
%      References sit side by side along the bottom edge.
%
%  SPACING -- measured, not guessed.  colspace and subcolspace have DIFFERENT
%  offsets in tikzposter: the subcolumn gutter is roughly subcolspace + 0.61in,
%  while the outer gutter is roughly colspace - 24pt.  Hence the asymmetric
%  values below, which were tuned by compiling and measuring block edges:
%      subcolspace=12pt -> 0.78 in gutter   (left | middle)
%      colspace=84pt    -> 0.75 in gutter   (middle | right)
%  innermargin has NO measurable effect on either gutter -- it was the previous
%  suspect and is not the cause.  Do not "fix" it.
% =====================================================================

\documentclass[a0paper,portrait,margin=0mm,colspace=84pt,subcolspace=12pt,blockverticalspace=40pt,innermargin=0pt]{tikzposter}
\geometry{paperwidth=36in,paperheight=36in,margin=0mm}

\usepackage[latin9]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\usepackage{multicol}
% \usepackage[colalign]{aligncolsatbottom}
% ^ DISABLED (Phase V6): aligncolsatbottom is not on CTAN and is not in the
% MiKTeX package database, so this line is a hard build failure, not the
% "untested interaction" the original header anticipated. Its only effect was
% cosmetic bottom-alignment of columns. Restore only if you obtain the .sty.

\setlist[itemize]{leftmargin=*,itemsep=2pt,topsep=1pt,parsep=0pt,partopsep=0pt}

% Body font for block text. tikzposter's a0 \normalsize is 24.88pt/30pt, which
% overflowed the bottom margin in the right column (measured: content reached
% 35.60 in against a 35.20 in limit). 22.8pt/27.4pt was found by compiling and
% measuring: it fills the page and leaves References and Contact bottoms within
% 0.15 in of each other. Change in ONE place to trade text against figure size.
\newcommand{\bodyfont}{\fontsize{22.8}{27.4}\selectfont}

%......................................................................
\tikzposterlatexaffectionproofoff
\usetikzlibrary{shapes.geometric,arrows.meta,positioning}

\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\definecolor{MyFadedNavy}{rgb}{0.224, 0.259, 0.310}
\definecolor{MyTerracotta}{rgb}{0.914, 0.310, 0.216}
\definecolor{MyIvory}{rgb}{0.965, 0.969, 0.922}

\usetheme{Default}
\definecolorstyle{MyStyle2016}{
	\definecolor{ColorOne}{named}{MyFadedNavy}
	\definecolor{ColorTwo}{named}{MyTerracotta}
	\definecolor{ColorThree}{named}{MyIvory}
}{
    \colorlet{titlebgcolor}{ColorOne}
    \colorlet{titlefgcolor}{white}
    \colorlet{backgroundcolor}{ColorOne!15}
    \colorlet{framecolor}{ColorOne}
    \colorlet{blocktitlebgcolor}{white}
    \colorlet{blocktitlefgcolor}{ColorTwo}
    \colorlet{blockbodybgcolor}{white}
    \colorlet{blockbodyfgcolor}{black}
    \colorlet{innerblocktitlebgcolor}{ColorOne!15}
    \colorlet{innerblocktitlefgcolor}{black}
    \colorlet{innerblockbodybgcolor}{ColorOne!15}
    \colorlet{innerblockbodyfgcolor}{black}
    \colorlet{notebgcolor}{ColorTwo!20}
    \colorlet{notefgcolor}{ColorTwo}
    \colorlet{notefrcolor}{ColorTwo}
 }

\usecolorstyle{MyStyle2016}
%......................................................................
\title{\parbox{0.92\linewidth}{\centering What Can a Persistence Diagram See?\\[0.3cm]
{\Large Invariance Structure and the Detectability of Data Poisoning}}}

\author{\underline{Christian Dane Beels}, Dr.\ Joseph Dorta}

\institute{Department of Mathematical Sciences\\United States Military Academy, West Point, NY.}

%......................................................................
\begin{document}

\maketitle[width=0.96\linewidth,titletoblockverticalspace=24pt,linewidth=0,roundedcorners=10]

\begin{columns}
%======================================================================
%  WIDE LEFT COLUMN (0.66) -- two subcolumns, then the References strip
%======================================================================
\column{0.66}

\begin{subcolumns}
%----------------------------------------------------------------------
\subcolumn{0.49}

\block[titleleft,roundedcorners=16]{Motivation}{
	\bodyfont\raggedright
	\begin{itemize}
		\item \textbf{Data poisoning} corrupts ML-based Network Intrusion Detection Systems (NIDS) by altering their training data.
		\item The defense must run \textbf{before training}, so clustering is unsupervised --- there are no labels to use.
		\item Monkam et al.\ [1] use TDA as that filter: bytes $\to$ image $\to$ cubical persistence $\to$ clustering. \textbf{What can that lens see?}
	\end{itemize}
}

\block[titleleft,roundedcorners=16]{Related Work}{
	\bodyfont\raggedright
	\begin{itemize}
		\item \textbf{Cohen-Steiner et al.\ (2005) [3].} Diagrams are stable: small perturbations give small bottleneck change. The warrant for topology as a detector.
		\item \textbf{Monkam, De Lucia \& Bastian (2024) [1].} The pipeline reconstructed here; reports $40$--$70\%$ of poison removed.
		\item \textbf{Ferrara (2025) [2].} Thm.\ 2 couples classification error to topological distortion --- under hypotheses (linear classifiers, Vietoris--Rips, Frobenius balls) that all fail here. A complementary axis: \emph{which points} to attack, where we ask \emph{which algebra} registers.
	\end{itemize}
}

\block[titleleft,roundedcorners=16]{Research Question}{
	\raggedright
	\innerblock{}{
		\centering
		\textbf{What determines a data poisoning attack's topological detectability --- and can a more sophisticated attack lead to less detectable topological signatures?}
	}
}

%----------------------------------------------------------------------
\subcolumn{0.51}

\block[titleleft,roundedcorners=16]{Methods I: An Invariance Argument}{
	\bodyfont\raggedright
	A swap attack applies $\sigma \in S_{1500}$ to the payload $x \in \{0,\dots,255\}^{1500}$. Binarization $b$ is pointwise, so it commutes with $\sigma$:

	\innerblock[bodywidth=0.86\linewidth]{}{
	\[
		\underbrace{b(x \circ \sigma) = b(x) \circ \sigma}_{\text{equivariance}}
		\ \ \Longrightarrow\ \
		\underbrace{\sum_{i} b\big((x \circ \sigma)_i\big) = \sum_{i} b(x_i)}_{\text{count is invariant}}
	\]
	}

	\begin{itemize}
		\item \textbf{Equivariance is not invariance.} Binarization commutes; the \emph{count} is what is fixed. Both steps are needed, and the result holds for every $\sigma$ --- not just the ${\sim}60$ transpositions applied.
		\item Every count-derived feature is therefore blind to permutation. Detection must flow through the \textbf{position-dependent} filtrations (Height, Radial). Invariance that is too coarse is a known hazard --- the motivation for group-equivariant operators [4].
		\item The threshold is a fraction of the batch maximum --- data-dependent, but a maximum is itself permutation-invariant. \textbf{Verified in code}, not assumed.
	\end{itemize}

	\begin{center}
	\includegraphics[width=\linewidth]{Figures/figure_v2_binarized_triptych}
	\end{center}

	\innerblock{}{
		\centering
		\textbf{The count channel is provably insensitive to permutation; the position channel stays open.}\\
		Attenuated on the foreground-count channel --- not absent.
	}
}

\block[titleleft,roundedcorners=16]{Methods II: Design}{
	\bodyfont\raggedright
	\begin{itemize}
		\item UNSW-NB15, $5{,}000$ packets, $10\%$ poison, $5$ seeds. Each $1500$-byte payload $\to$ $30 \times 50$ image; $5$ filtrations $\times$ $6$ vectorizations $\times$ $2$ homology dimensions $= 60$ features (reduced homology) --- the binarize--filtration--cubical pattern of Garin \& Tauzin [5].
		\item \textbf{Capture} $=$ poison landing in \emph{$100\%$-pure} clusters. Zero-tolerance: a $99\%$-poison cluster counts for nothing.
		\item OPTICS reported throughout. DBSCAN and MeanShift $\approx 0$; HDBSCAN is nonzero only on cyclic shift, at $3/5$ seeds.
		\item Four multiset-preserving families, so every poisoned packet stays well-formed, with Gaussian noise as positive control.
	\end{itemize}
}

\end{subcolumns}
%----------------------------------------------------------------------
%  REFERENCES -- spans the two subcolumns above, not the right column.
%  Sits at bottom-left, opposite the Contact block at bottom-right.
%----------------------------------------------------------------------
\block[titleleft,roundedcorners=16]{References}{
\small
\begin{multicols}{2}
\begin{itemize}[itemsep=2pt,leftmargin=*]
	\item[{[1]}] G. F. Monkam, M. J. De Lucia, and N. D. Bastian, ``A topological data analysis approach for detecting data poisoning attacks against machine learning based network intrusion detection systems,'' \emph{Computers \& Security}, vol.\ 144, art.\ no.\ 103929, Sep.\ 2024.
	\item[{[2]}] M. Ferrara, ``Modeling by topological data analysis and game theory for analyzing data poisoning phenomena,'' \emph{AIMS Mathematics}, vol.\ 10, no.\ 7, pp.\ 15457--15475, Jul.\ 2025.
	\item[{[3]}] D. Cohen-Steiner, H. Edelsbrunner, and J. Harer, ``Stability of persistence diagrams,'' in \emph{Proc.\ 21st Annu.\ Symp.\ Computational Geometry (SCG '05)}, Pisa, Italy, Jun.\ 2005, pp.\ 263--271.
	\item[{[4]}] M. G. Bergomi, P. Frosini, D. Giorgi, and N. Quercioli, ``Towards a topological--geometrical theory of group equivariant non-expansive operators for data analysis and machine learning,'' \emph{Nature Machine Intelligence}, vol.\ 1, pp.\ 423--433, 2019.
	\item[{[5]}] A. Garin and G. Tauzin, ``A topological reading lesson: Classification of MNIST using TDA,'' arXiv:1910.08345, 2019.
\end{itemize}
\end{multicols}
}

%======================================================================
%  RIGHT COLUMN (0.34) -- runs to the bottom, ending with Contact
%======================================================================
\column{0.34}

\block[titleleft,roundedcorners=16]{Results: Four Families, One Proof}{
	\bodyfont\raggedright
	\begin{center}
	\includegraphics[width=\linewidth]{Figures/figure_v3_four_families}
	\end{center}

	\begin{itemize}
		\item \textbf{The count never moved:} $0/200$ for all four families, across $1{,}600$ checks at two thresholds --- zero exceptions.
		\item \textbf{Identical invariance, a six-point spread in capture.} The invariance class alone does not determine detectability.
		\item \textbf{Random swaps are quiet for a computable reason:} foreground fraction $p \approx 0.042$, so only $2p(1-p) \approx 8\%$ of transpositions change the image. Control: noise shifts the count by $|\Delta| = 14.02$.
	\end{itemize}
}

\block[titleleft,roundedcorners=16]{Discussion}{
	\bodyfont\raggedright
	\begin{itemize}
		\item \textbf{Capture tracks spatial disruption, not sophistication:} unguided cyclic shift matches surrogate-guided search.
		\item \textbf{A reported negative result.} Guidance gave $+4.68 \pm 1.57$ ($5/5$ seeds) on UNSW-NB15 but \textbf{does not replicate} on CICIDS2017: $-0.60 \pm 3.03$, $2/5$ seeds, per-seed $[-0.4, -2.2, +0.4, +4.2, -5.0]$ --- the sign flips and the mean crosses zero. Pre-committed; not rescued.
		\item The invariance is elementary folklore; we find no prior statement of it for this pipeline. Scope: \emph{multiset-preserving} perturbations. Whether \emph{functional} reorderings in the same group --- header order, padding placement --- inherit the attenuation is a conjecture this motivates, \textbf{not a result}.
	\end{itemize}
}

\block[titleleft,roundedcorners=16]{Conclusions}{
	\bodyfont\raggedright
	\begin{itemize}
		\item One channel of the detector is \textbf{provably insensitive} to permutation attacks. Stability [3] bounds how far a diagram can move; it gives no lower bound --- a large attack can move it \emph{not at all}.
		\item \textbf{Insensitive is not undetectable:} four identically invariant families span $0\%$--$6.3\%$.
		\item Detectability is a property of the \textbf{(attack, detector) pair} --- blind spots \emph{arising from feature-map invariances} can be read off the algebra before any experiment. The algebra does not characterise every failure mode.
	\end{itemize}

	\innerblock{}{
		\small
		\textbf{Limitations.} Proof: any fixed-threshold pipeline. Empirics: one reconstruction, one primary dataset, two thresholds, $5$ seeds; cross-dataset replication attempted and failed.
	}
}

% Contact moved here (was bottom of the left subcolumn) so that it sits at
% bottom-right, opposite the References strip at bottom-left.
\block[titleleft,roundedcorners=16]{Contact}{
	\small
	\raggedright
	\textbf{Christian Dane Beels} \quad christian.beels@westpoint.edu\\
	github.com/cdaneb/ \quad linkedin.com/in/christiandanebeels/\\
	Dept.\ of Mathematical Sciences, USMA West Point
}

\end{columns}
\end{document}